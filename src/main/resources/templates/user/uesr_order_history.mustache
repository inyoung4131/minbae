<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <script>
        const b_back = () => {
            location.href = document.referrer;
        }

        const reviewCreate = () => {

        }
    </script>
</head>
<body>
    <div><a href="javascript:b_back()"><-</a></div>
    <hr>
    <div>
        {{#orderHistoryList}}
        <div>주문 날짜:{{order_date}}</div>
        <div>가게 이미지:{{store_detail_info_img}}</div>
        <div>가게 이름:{{store_name}}</div>
        <div>총 주문 수량:{{totalCount}}</div>
        <div>총 금액:{{order_price}}</div>
        <div>
            <a href="javascript:reviewCreate();">리뷰 작성하기</a>
        </div>
        <hr>
        {{/orderHistoryList}}
    </div>
</body>
</html>